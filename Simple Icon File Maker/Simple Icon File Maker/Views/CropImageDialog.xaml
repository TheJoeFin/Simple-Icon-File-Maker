<ContentDialog
    x:Class="Simple_Icon_File_Maker.CropImageDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Crop Image"
    CloseButtonText="Cancel"
    IsPrimaryButtonEnabled="False"
    Loaded="ContentDialog_Loaded"
    PrimaryButtonText="Apply"
    mc:Ignorable="d">

    <Grid MinWidth="560" MinHeight="500" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Text="Drag the handles to select the crop region"
            TextWrapping="Wrap" />

        <Grid Grid.Row="1" MinHeight="460">
            <ProgressRing
                x:Name="LoadingRing"
                Width="32"
                Height="32"
                IsActive="True" />
            <controls:ImageCropper
                x:Name="ImageCropperControl"
                Visibility="Collapsed" />
            <InfoBar
                x:Name="ErrorInfoBar"
                IsClosable="False"
                IsOpen="False"
                Severity="Error"
                VerticalAlignment="Bottom" />
        </Grid>
    </Grid>
</ContentDialog>
